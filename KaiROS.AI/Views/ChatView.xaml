<UserControl x:Class="KaiROS.AI.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:KaiROS.AI.ViewModels"
             xmlns:converters="using:KaiROS.AI.Converters">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolConverter x:Key="InverseBool"/>
        <converters:MarkdownContentConverter x:Key="MarkdownConverter"/>
        <converters:StringFormatConverter x:Key="StringFmt"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Session Sidebar -->
        <Border Background="{StaticResource CardBrush}"
                CornerRadius="12"
                Margin="0,0,16,0"
                Padding="12"
                Width="220"
                Visibility="{Binding IsSessionListVisible, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- New Chat Button -->
                <Button Command="{Binding NewSessionCommand}"
                        Style="{StaticResource AccentButton}"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE710;" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="New Chat" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <!-- Session List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Sessions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource SurfaceLightBrush}"
                                        CornerRadius="8"
                                        Padding="12,10"
                                        Margin="0,0,0,8"
                                        Tapped="SessionItem_Tapped">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" 
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       FontWeight="Medium"/>
                                            <TextBlock Foreground="{StaticResource TextMutedBrush}" 
                                                       FontSize="11"
                                                       Margin="0,4,0,0">
                                                <Run Text="{Binding MessageCount, Mode=OneWay}"/>
                                                <Run Text="messages &#x2022;"/>
                                                <Run Text="{Binding UpdatedAt, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" 
                                                Style="{StaticResource IconButton}"
                                                Click="DeleteSession_Click"
                                                Tag="{Binding}"
                                                VerticalAlignment="Center"
                                                Opacity="0.6">
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" FontSize="13"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Empty State -->
                <TextBlock Grid.Row="1"
                           Text="No chat history yet"
                           Foreground="{StaticResource TextMutedBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding Sessions.Count, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
            </Grid>
        </Border>
        
        <!-- Main Chat Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
        
        <!-- Header with Model Info -->
        <Border Background="{StaticResource CardBrush}" 
                CornerRadius="12" 
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="Chat" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding ActiveModelInfo}" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding ClearChatCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE74D;" FontSize="13" VerticalAlignment="Center"/>
                            <TextBlock Text=" Clear" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ToggleSearchCommand}"
                            Style="{StaticResource SecondaryButton}"
                            ToolTipService.ToolTip="Search (Ctrl+F)"
                            Margin="0,0,8,0">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE721;" FontSize="14"/>
                    </Button>
                    <!-- Search Bar (collapsible) -->
                    <Border Visibility="{Binding IsSearchVisible, Converter={StaticResource BoolToVis}}"
                            Background="{StaticResource SurfaceLightBrush}"
                            CornerRadius="8"
                            Padding="8,4"
                            Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"
                                     Width="150"
                                     Padding="4"
                                     VerticalAlignment="Center"/>
                            <Button Command="{Binding CloseSearchCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    Padding="4,2"
                                    Margin="4,0,0,0">
                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="10"/>
                            </Button>
                        </StackPanel>
                    </Border>
                    <Button x:Name="ExportButton" 
                            Style="{StaticResource SecondaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A9;" FontSize="13" VerticalAlignment="Center"/>
                            <TextBlock Text=" Export" VerticalAlignment="Center"/>
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70D;" FontSize="10" Opacity="0.7" Margin="4,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button.Flyout>
                            <MenuFlyout x:Name="ExportMenu">
                                <MenuFlyoutItem Text="Export as Markdown (.md)" 
                                                Command="{Binding ExportChatAsMarkdownCommand}"/>
                                <MenuFlyoutItem Text="Export as JSON (.json)" 
                                                Command="{Binding ExportChatAsJsonCommand}"/>
                                <MenuFlyoutItem Text="Export as Text (.txt)" 
                                                Command="{Binding ExportChatAsTextCommand}"/>
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Messages -->
        <ScrollViewer Grid.Row="1" 
                      x:Name="MessagesScroller"
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,16">
            <ItemsControl ItemsSource="{Binding Messages}" x:Name="MessagesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <!-- User Message -->
                            <Border HorizontalAlignment="Right"
                                    MaxWidth="600"
                                    Visibility="{Binding IsUser, Converter={StaticResource BoolToVis}}">
                                <Border Background="{StaticResource PrimaryBrush}" 
                                        CornerRadius="16,16,4,16" 
                                        Padding="16,12">
                                    <StackPanel>
                                        <!-- Attached Image Display -->
                                        <Image Source="{Binding AttachedImagePath}"
                                               MaxWidth="400" MaxHeight="300"
                                               Margin="0,0,0,8"
                                               Stretch="Uniform"
                                               Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}"/>

                                        <TextBlock Text="{Binding Content}" 
                                                   Foreground="White"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Timestamp}" 
                                                   Foreground="#FFFFFF80"
                                                   FontSize="11"
                                                   HorizontalAlignment="Right"
                                                   Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Border>
                            
                            <!-- Assistant Message -->
                            <Border HorizontalAlignment="Left"
                                    MaxWidth="600"
                                    Visibility="{Binding IsAssistant, Converter={StaticResource BoolToVis}}">
                                <Border Background="{StaticResource SurfaceLightBrush}" 
                                        CornerRadius="16,16,16,4" 
                                        Padding="16,12">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Text="&#xE9CE; Assistant" 
                                                       Foreground="{StaticResource PrimaryLightBrush}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                            
                                            <!-- Copy Button -->
                                            <Button  Grid.Column="1"
                                                     Command="{Binding CopyContentCommand}"
                                                     Style="{StaticResource IconButton}"
                                                     Width="24" Height="24"
                                                     Padding="4"
                                                     ToolTipService.ToolTip="Copy Response">
                                                <Path Data="M19,21H8V7H19M19,5H8A2,2 0 0,0 6,7V21A2,2 0 0,0 8,23H19A2,2 0 0,0 21,21V7A2,2 0 0,0 19,5M16,1H4A2,2 0 0,0 2,3V17H4V3H16V1Z"
                                                      Fill="{StaticResource TextSecondaryBrush}"
                                                      Stretch="Uniform"/>
                                            </Button>
                                        </Grid>
                                        <ContentPresenter Content="{Binding Content, Converter={StaticResource MarkdownConverter}}"/>
                                        <TextBlock Text="{Binding Timestamp}" 
                                                   Foreground="{StaticResource TextMutedBrush}"
                                                   FontSize="11"
                                                   HorizontalAlignment="Right"
                                                   Margin="0,8,0,0"/>
                                        
                                        <!-- Streaming Indicator -->
                                        <TextBlock Text="&#x2022;&#x2022;&#x2022;" 
                                                   Foreground="{StaticResource PrimaryBrush}"
                                                   FontSize="14"
                                                   Margin="0,4,0,0"
                                                   Visibility="{Binding IsStreaming, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Input Area -->
        <Border Grid.Row="2" 
                Background="{StaticResource CardBrush}" 
                CornerRadius="12" 
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.ColumnSpan="2" Margin="0,0,0,8">
                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                         <!-- Knowledge Base Selector -->
                         <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                             <TextBlock Text="Knowledge Base"
                                        VerticalAlignment="Center"
                                        Margin="0,0,8,0"
                                        Foreground="{StaticResource TextSecondaryBrush}"
                                        FontSize="12"/>
                             <ComboBox ItemsSource="{Binding AvailableKnowledgeBases}" 
                                       SelectedItem="{Binding SelectedKnowledgeBase, Mode=TwoWay}"
                                       Width="180"
                                       Padding="6,4"
                                       VerticalContentAlignment="Center"/>
                         </StackPanel>

                     <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                         <TextBlock Text="Web Search"
                                    VerticalAlignment="Center"
                                    Margin="0,0,8,0"
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    FontSize="12"/>
                         <ToggleSwitch IsOn="{Binding IsWebSearchEnabled, Mode=TwoWay}"
                                      ToolTipService.ToolTip="Enable Web Search to fetch real-time info"/>
                     </StackPanel>

                     <!-- Enter to Send Toggle -->
                     <StackPanel Orientation="Horizontal">
                         <TextBlock Text="Enter to Send"
                                    VerticalAlignment="Center"
                                    Margin="0,0,8,0"
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    FontSize="12"/>
                         <ToggleSwitch IsOn="{Binding IsEnterToSendEnabled, Mode=TwoWay}"
                                      ToolTipService.ToolTip="Press Enter to send message"/>
                     </StackPanel>
                 </StackPanel>
                     
                     <!-- Attached Document Indicator -->
                     <Border HorizontalAlignment="Left" 
                             Background="{StaticResource SurfaceBrush}" 
                             CornerRadius="4" 
                             Padding="8,4" 
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Margin="0,0,8,0"
                             Visibility="{Binding CurrentDocumentName, Converter={StaticResource BoolToVis}}">
                         <StackPanel Orientation="Horizontal">
                             <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A5;" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                             <TextBlock Text="{Binding CurrentDocumentName}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Medium"/>
                             <Button Command="{Binding RemoveDocumentCommand}"
                                     Style="{StaticResource SecondaryButton}"
                                     Padding="4,0"
                                     Margin="8,0,0,0"
                                     Height="16"
                                     ToolTipService.ToolTip="Remove attached document"><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="10"/></Button>
                         </StackPanel>
                     </Border>

                     <!-- Attached Image Indicator -->
                     <Border HorizontalAlignment="Left" 
                             Background="{StaticResource SurfaceBrush}" 
                             CornerRadius="4" 
                             Padding="8,4" 
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Visibility="{Binding HasAttachedImage, Converter={StaticResource BoolToVis}}">
                         <StackPanel Orientation="Horizontal">
                             <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEB9F;" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                             <TextBlock Text="Image Attached" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Medium"/>
                             <Button Command="{Binding RemoveAttachedImageCommand}"
                                     Style="{StaticResource SecondaryButton}"
                                     Padding="4,0"
                                     Margin="8,0,0,0"
                                     Height="16"
                                     ToolTipService.ToolTip="Remove attached image"><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="10"/></Button>
                         </StackPanel>
                     </Border>
                </Grid>
                
                <TextBox x:Name="MessageInput"
                         Grid.Row="1"
                         Text="{Binding UserInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MaxHeight="150"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ToolTipService.ToolTip="Ctrl+Enter to send (or Enter if enabled)"
                         AllowDrop="True"
                         DragOver="MessageInput_DragOver"
                         Drop="MessageInput_Drop"
                         KeyDown="MessageInput_KeyDown">
                    <TextBox.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="Enter" Invoked="SendAccelerator_Invoked"/>
                        <KeyboardAccelerator Modifiers="Control" Key="N" Invoked="NewSessionAccelerator_Invoked"/>
                        <KeyboardAccelerator Modifiers="Control" Key="L" Invoked="ClearChatAccelerator_Invoked"/>
                        <KeyboardAccelerator Modifiers="Control" Key="F" Invoked="ToggleSearchAccelerator_Invoked"/>
                    </TextBox.KeyboardAccelerators>
                </TextBox>
                
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Bottom">
                    <!-- Upload Image Button -->
                    <Button Command="{Binding AttachImageCommand}"
                            Style="{StaticResource SecondaryButton}"
                            ToolTipService.ToolTip="Attach Image (Vision Model Required)"
                            Margin="0,0,8,0"
                            Padding="10,6"
                            Visibility="{Binding ModelManager.IsVisionModelLoaded, Converter={StaticResource BoolToVis}}"><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEB9F;" FontSize="16"/></Button>

                    <!-- Upload Document Button -->
                    <Button Command="{Binding UploadDocumentCommand}"
                            Style="{StaticResource SecondaryButton}"
                            ToolTipService.ToolTip="Attach text document"
                            Margin="0,0,8,0"
                            Padding="10,6"><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A5;" FontSize="16"/></Button>

                    <!-- Send Button -->
                    <Button Command="{Binding SendMessageCommand}"
                            IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBool}}"
                            Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"
                            Style="{StaticResource PrimaryButton}"
                            ><StackPanel Orientation="Horizontal"><TextBlock Text="Send" VerticalAlignment="Center"/><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE724;" FontSize="14" Margin="6,0,0,0" VerticalAlignment="Center"/></StackPanel></Button>
                    
                    <!-- Stop Button -->
                    <Button Command="{Binding StopGenerationCommand}"
                            Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}}"
                            Style="{StaticResource SecondaryButton}"
                            BorderBrush="{StaticResource ErrorBrush}"
                            ><StackPanel Orientation="Horizontal"><FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE71A;" FontSize="14" VerticalAlignment="Center"/><TextBlock Text=" Stop" VerticalAlignment="Center"/></StackPanel></Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Performance Stats -->
        <Border Grid.Row="3" 
                Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8" 
                Padding="12"
                Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xE945;"/>
                    <Run Text="{Binding TokensPerSecond, Converter={StaticResource StringFmt}, ConverterParameter='{}{0:F1}'}"/>
                    <Run Text="tok/s"/>
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="20,0,0,0">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xE9F3;"/>
                    <Run Text="{Binding TotalTokens}"/>
                    <Run Text="tokens"/>
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="20,0,0,0">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xE950;"/>
                    <Run Text="{Binding MemoryUsage}"/>
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="20,0,0,0">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xE916;"/>
                    <Run Text="{Binding ElapsedTime}"/>
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="20,0,0,0">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xE8EF;"/>
                    <Run Text="Ctx:"/>
                    <Run Text="{Binding ContextWindow}"/>
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="20,0,0,0">
                    <Run FontFamily="Segoe MDL2 Assets" Text="&#xECE4;"/>
                    <Run Text="GPU:"/>
                    <Run Text="{Binding GpuLayers}"/>
                    <Run Text="layers"/>
                </TextBlock>
            </StackPanel>
        </Border>
        </Grid>
    </Grid>
</UserControl>

